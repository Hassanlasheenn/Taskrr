worker_processes auto;

events {
    worker_connections 2048; 
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;
    client_max_body_size 10M;

    log_format load_balancer_logs '$remote_addr - $remote_user [$time_local] '
                                  '"$request" $status $body_bytes_sent '
                                  '"$http_referer" "$http_user_agent" '
                                  'TO_BACKEND: $upstream_addr '
                                  'RT: $upstream_response_time';

    access_log /var/log/nginx/access.log load_balancer_logs;

    server_tokens off;

    map $http_upgrade $connection_upgrade {
        default upgrade;
        ''      close;
    }

    upstream backend_servers {
        server backend:8000;
    }

    # HTTP Server - Redirect everything to HTTPS
    server {
        listen 80;
        server_name taskrr.app www.taskrr.app;

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location / {
            return 301 https://$host$request_uri;
        }
    }

    # HTTPS Server
    server {
        listen 443 ssl;
        server_name taskrr.app www.taskrr.app;

        # SSL Certificates (managed by Certbot)
        ssl_certificate /etc/letsencrypt/live/taskrr.app/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/taskrr.app/privkey.pem;

        # Security Headers
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-XSS-Protection "1; mode=block" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        add_header X-Served-By $upstream_addr always;

        gzip on;
        gzip_types application/json text/plain text/css application/javascript;

        resolver 127.0.0.11 valid=5s;

        location /nginx-health {
            return 200 '{"status":"ok"}';
        }

        # Static files from backend (profile pictures)
        location /static/ {
            proxy_pass http://backend_servers;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Host $host;
        }

        # Backend API Proxy
        # ONLY proxy specific API paths. 
        # /verify-email and /resend-verification are shared between FE and BE.
        location ~ ^/(todos|users|notifications|health|admin/) {
            proxy_pass http://backend_servers;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Host $host;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;

            # Disable browser caching for API responses
            add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0" always;
            add_header Pragma "no-cache" always;
            add_header Expires "0" always;
            add_header Vary "Accept, Authorization" always;
        }

        # Special handling for routes that exist in both FE and BE or have similar prefixes
        # If it's a POST or has a specific API header, we proxy to backend
        location ~ ^/(login|register|logout|verify-email|resend-verification) {
            # 1. If it's a POST, it's always the API
            if ($request_method = POST) {
                add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0" always;
                proxy_set_header Host $host;
                proxy_set_header X-Real-IP $remote_addr;
                proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
                proxy_set_header X-Forwarded-Proto $scheme;
                proxy_set_header X-Forwarded-Host $host;
                proxy_pass http://backend_servers;
            }
            
            # 2. For GET requests (like verify-email), check if it's an API call (JSON)
            # Browser navigation usually has 'text/html' in Accept header
            # API calls from Angular have 'application/json'
            if ($http_accept ~* "application/json") {
                add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0" always;
                proxy_set_header Host $host;
                proxy_set_header X-Real-IP $remote_addr;
                proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
                proxy_set_header X-Forwarded-Proto $scheme;
                proxy_set_header X-Forwarded-Host $host;
                proxy_pass http://backend_servers;
            }

            # 3. Otherwise, serve the frontend index.html
            root /var/www/frontend;
            try_files $uri $uri/ /index.html;
            add_header Cache-Control "no-cache, must-revalidate" always;
        }

        # Frontend Root
        location / {
            root /var/www/frontend;
            try_files $uri $uri/ /index.html;
            add_header Cache-Control "no-cache, must-revalidate" always;
            add_header Vary "Accept" always;
        }
    }
}
